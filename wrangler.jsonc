{
  // https://developers.cloudflare.com/pages/framework-guides/deploy-a-nuxt-site/
  // NOTE: nitro's cloudflare-pages preset generates its own wrangler config at
  // dist/_worker.js/wrangler.json and overrides pages_build_output_dir.
  // Actual build output is dist/ — Cloudflare Pages handles deploy automatically,
  // no custom deploy command should be set in the dashboard.
  "name": "dna-barcode-generator",
  "compatibility_date": "2025-01-01"
}
